#include <stdio.h>

void main(){
    /* puts writes every character from the null-terminated string str
       and one additional newline character '\n' to the output stream stdout */
    puts("Hello World");
}